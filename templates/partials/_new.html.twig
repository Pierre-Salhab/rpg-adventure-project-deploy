{% extends 'base.html.twig' %}

{% block title %}New {{ entity }}
{% endblock %}

{% block body %}
	<div class="container my-4">
		<h1 class='text-center'>New {{ entity }}</h1>
		<div class="row">
			<div class="col-md-8 offset-md-2">
				<div class="card">
					<div class="card-body">

						{{ include('partials/_form.html.twig') }}

						<a class='btn btn-primary show mt-1' href="{{ path('app_' ~ entity ~ '_index') }}">Retour à la liste</a>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}

