{% extends 'base.html.twig' %}

{% block body %}
	<div class="container my-4">
		<h1 class="text-center">
			{% block title %}
				{{ item.name }}
			{% endblock %}
		</h1>
		<div class="row">
			<div class="col-md-8 offset-md-2">
				<div class="card">
					<div class="card-body">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td><img class="img-fluid" src="{{ item.picture }}" alt="{{ 'image de ' ~ item.name }}">{{ item.picture }}</td>
            </tr>
            <tr>
                <th>Health</th>
                <td>{{ item.health }}</td>
            </tr>
            <tr>
                <th>Strength</th>
                <td>{{ item.strength }}</td>
            </tr>
            <tr>
                <th>Intelligence</th>
                <td>{{ item.intelligence }}</td>
            </tr>
            <tr>
                <th>Dexterity</th>
                <td>{{ item.dexterity }}</td>
            </tr>
            <tr>
                <th>Defense</th>
                <td>{{ item.defense }}</td>
            </tr>
            <tr>
                <th>Karma</th>
                <td>{{ item.karma }}</td>
            </tr>
            <tr>
                <th>Xp</th>
                <td>{{ item.xp }}</td>
            </tr>
        </tbody>
    </table>
<a class='btn btn-warning  m-1' href="{{ path('app_item_edit', {'id': item.id}) }}">Modifier</a>
						{{ include('item/_delete_form.html.twig') }}
						<a class='btn btn-primary  m-1' href="{{ path('app_item_index') }}">Retour à la liste</a>

					</div>
				</div>

			</div>
		</div>

	</div>
{% endblock %}
