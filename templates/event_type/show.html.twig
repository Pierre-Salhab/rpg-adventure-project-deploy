{% extends 'base.html.twig' %}


{% block body %}
	<div class="container my-4">
		<h1 class="text-center">
			{% block title %}
				Type : {{ event_type.name }}
			{% endblock %}
		</h1>
		<div class="row">
			<div class="col-md-8 offset-md-2">
				<div class="card">
					<div class="card-body">
						<table class="table">
							<tbody>
								<tr>
									<th>Id</th>
									<td>{{ event_type.id }}</td>
								</tr>
								<tr>
									<th>Name</th>
									<td>{{ event_type.name }}</td>
								</tr>
							</tbody>
						</table>
						<a class='btn btn-warning m-1' href="{{ path('app_event_type_edit', {'id': event_type.id}) }}">Modifier</a>
						{{ include('event_type/_delete_form.html.twig') }}
						<a class='btn btn-primary m-1' href="{{ path('app_event_type_index') }}">Retour à la liste</a>

					</div>
				</div>

			</div>
		</div>

	</div>
{% endblock %}
