<form method="post" action="{{ path('app_dialogue_delete', {'id': dialogue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dialogue.id) }}">
    <button class="btn btn-danger float-end m-1">Delete</button>
</form>
