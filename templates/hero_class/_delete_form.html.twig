<form method="post" action="{{ path('app_hero_class_delete', {'id': hero_class.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hero_class.id) }}">
    <button class="btn btn-danger float-end m-1">Supprimer</button>
</form>
